# GRUB configuration for LOS

set timeout=3
set default=0

menuentry "LOS - Lambda Operating System" {
    multiboot2 /boot/los-kernel
    boot
}

menuentry "LOS (Serial Console)" {
    multiboot2 /boot/los-kernel serial
    boot
}
